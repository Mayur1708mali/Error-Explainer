# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Databases and generated RAG data (sqlite db is rebuilt from chunks.json)
*.db
*.db-shm
*.db-wal

# The pre-indexed doc corpus (chunks.json + docs/) ships with the repo.
# Only the generated SQLite database is ignored above.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
